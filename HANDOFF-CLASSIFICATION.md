# סיווג הצעות חוק לצירים — מצב והמשך

נכתב 19.9.2026, בסוף סשן ארוך עם דינה. הקובץ נועד לאפשר לסשן חדש להמשיך
בלי לקרוא את כל ההיסטוריה.

---

## 1. המצב במספרים

```
הצעות חוק במסד        7,296
מסווגות               5,603
שיוכים (bill↔ציר)     6,169
יתומות עם יוזם        1,020
צירים בשאלון            182
אשכולות                  53
נושאי-על                  8
```

הכול על ענף `dina`, שזהה ל-`origin/dev`. **המסד לא נגע בשינויים הגדולים** —
1,969 שינויים מוצעים ממתינים באקסל ולא הוחלו.

---

## 2. ההחלטה המנחה של דינה

> **מהימנות לפני כיסוי.**

שיוך שגוי גרוע מיתמות: הוא מזכה ח"כ על עבודה שלא עשה ומופיע בדירוג
שמישהי עשויה לפעול לפיו, בעוד הצעה יתומה רק אינה נספרת.

הצעתי את ההפך והיא דחתה בצדק. **כל החלטה עתידית צריכה להיגזר מזה.**

---

## 3. מה כבר הוחל ונדחף

| | |
|---|---|
| מערכת עיצוב נייבי-זהב-קלף | כל האתר |
| gemini-3.8-flash + חיוב מקושר | מכסה פתוחה, ~$0.75/מיליון קלט |
| תקרות שימוש ל-`/ask` | `ASK_MONTHLY_BUDGET=1200`, נכשל סגור |
| ניסוח השאלון לשני המינים | מקור עקיף, לא כפל צורות |
| פס הציון לפי סיעה | היה לפי דירוג וסתר את קוד הצבעים |
| 5 צירים חדשים | להט"ב ×3, כתות פוגעניות, ועוד |
| 467 + 356 הצעות שויכו | הצלת יתומות |
| **הסף להצגת ציר: 5 → 1** | מנגנון הביטחון עושה את העבודה בגילוי נאות |

---

## 4. הצינור שנבנה — ארבעה שלבים

כל שלב מודד דבר אחד ואינו כותב למסד.

```
scripts/audit-classifications.ts   ← האם ההצעה שייכת לציר שהיא בו?
scripts/rehome-wrong-axis.ts       ← אם לא, לאן כן?
scripts/verify-match-sides.ts      ← בעד או נגד, בבדיקה נפרדת
scripts/export-changes-xlsx.ts     ← הכול לאקסל להחלטה
```

**התוצאה:** 39% מהשיוכים שגויים. 2,212 נפסלו בשני פרומפטים בלתי תלויים.

```
867   עוברים לציר אחר
1,102 נמחקים
153   זוכו — הביקורת טעתה לגביהם
```

### שלוש הגנות, וכמה כל אחת הצילה

| ההגנה | הצילה |
|---|---|
| שלושה כללים שנוספו לביקורת אחרי בדיקה ידנית | **454** שיוכים |
| שלב 2 רשאי לאשר את הציר המקורי | **153** |
| אימות צד נפרד, תוויות מוסתרות וסדר מתחלף | **211** צדדים הפוכים |

בלי ההגנות היו נמחקים 1,556 ו-211 נכתבים בצד ההפוך.

---

## 5. הצעד הבא שסוכם

דינה גילתה, מתוך קריאת גיליון המחיקות, ש**רשימת המחיקות היא מפה של
החורים בטקסונומיה**. דוגמה:

> 25 הצעות "התפזרות הכנסת העשרים וחמש" נמחקות מהציר "האם להקשיח את תנאי
> פיזור הכנסת והדחת הממשלה?". הביקורת צודקת — הציר על *הכללים*, ההצעות הן
> *המעשה*. אבל התוצאה היא שהפעולה הפרלמנטרית הדרמטית ביותר נשארת בלי ייצוג.
>
> **הציר החסר: "האם לפזר את הכנסת וללכת לבחירות מוקדמות?"**

835 המחיקות מרוכזות ב-142 צירים. הראש של הרשימה:

```
35  האם להרחיב את ההגנות והזכויות הסוציאליות לעובדים?
29  האם לתת הטבות מס ממוקדות לאוכלוסיות ועסקים מסוימים?
25  האם להקשיח את תנאי פיזור הכנסת והדחת הממשלה?
19  האם לאפשר טיפולים רפואיים עצמאיים לפי שיקול דעת הפרט?
17  האם להחמיר את הפיקוח והרגולציה במערכת הבריאות?
```

### מה לבנות

`scripts/find-gap.ts` — מקבל ציר או מונחי חיפוש, ומחזיר את קבוצת ההצעות
שנפלטת ממנו, היוזמים, בדיקת כפילות מול 216 הצירים הקיימים, בדיקת
דו-צדדיות, והצעת ניסוח. דינה מאשרת, `--apply` יוצר ומשייך.

**כל ציר שנוצר מקטין את רשימת המחיקות** — ההצעות עוברות אליו במקום להימחק.

### המלצה שנאמרה ולא הוכרעה

**לא להחיל את המחיקות עכשיו.** חלק גדול ייפתר כשייווצרו הצירים החסרים.
את **המעברים (867) והזיכויים (153) אפשר להחיל כבר** — הם אינם תלויים בזה.

---

## 6. מלכודות שעלו בפועל — לא לחזור עליהן

- **`build-deploy-db.ts` לא העתיק את `bill_political_classification`.** הבדיקות
  שלו אישרו שהטבלה קיימת ושהצטרפות עובדת, ושתיהן עברו כשהתוכן היה מיושן
  בחודשיים. **תמיד להשוות ספירות, לא רק להריץ שאילתה.**

- **`MIN_BILLS_PER_ISSUE` הסתיר צירים בשקט.** נוצרו עשרה צירים עם 4 הצעות
  שנכתבו למסד ולא הופיעו בשאלון. הסף ירד ל-1, אבל **תמיד לוודא שציר שנוצר
  באמת מגיע ל-`CLUSTERS`.**

- **מזהי צירים הם גיבוב** (`ax_a9fb0eb99c`), לא שם קריא. ניחוש מזהה
  מהשאלה מייצר מזהה שאינו קיים, ו-`DELETE` עליו מצליח בלי למחוק כלום.

- **תת-מחרוזת בעברית מטעה.** `%להט%` תופס "להטיל", `%כת%` תופס "מכתב"
  (1,139 תוצאות). כל מונח חייב להיבדק לחוד ולדווח כמה תפס.

- **שני תווי גרשיים:** `"` של ASCII ו-`״` (U+05F4). הנתונים מערבבים.

- **`\b` ב-regex לא עובד על עברית ביוניקוד.** זיהוי "מילה שמתחילה ב-ו'"
  נכשל בשקט.

- **חקיקה בלשון ניטרלית לא נתפסת בחיפוש מילים.** 13 הצעות על אימוץ והורות
  הן מחסומים על זוגות חד-מיניים ואף אחת לא מזכירה להט"ב — הן כתובות
  "שני בני אדם" ו"בן או בת זוג" בכוונה.

- **שיפוט לפי כותרת מטעה.** "הרשות לפיתוח הנגב" התברר כהחלת חוק על יהודה
  ושומרון. **תמיד לקרוא את `policy_change`.**

- **`retries` על Gemini בולע מכסה.** בסקריפטים כבדים `retries: 0`, ותשובה
  לא תקפה לא נרשמת לקובץ כדי שההרצה החוזרת תדון בה שוב.

- **אקסל נועל קובץ פתוח** (EBUSY). נפילה שם מאבדת הרצה שלמה.

- **ולידציית אקסל:** בלי `=` מוביל, ולידציה אחת לטווח ולא לתא, גיליון
  המקור גלוי ולא `veryHidden`, ו-autoFilter לא על עמודות המילוי.

---

## 7. קבצים

### סקריפטים מרכזיים
```
scripts/audit-classifications.ts    ביקורת
scripts/rehome-wrong-axis.ts        מציאת בית
scripts/verify-match-sides.ts       אימות צד
scripts/export-changes-xlsx.ts      ייצוא השינויים
scripts/export-orphans-xlsx.ts      ייצוא היתומות
scripts/match-orphans-to-axes.ts    התאמת יתומות
scripts/add-cult-abuse-axis.ts      תבנית ליצירת ציר חדש
scripts/build-deploy-db.ts          בניית מסד הפרודקשן
```

### תוצרים (ב-gitignore)
```
שינויים-מוצעים-חדש.xlsx    1,969 שינויים — ממתין להחלטה
הצעות-לסיווג.xlsx          1,020 יתומות לסיווג ידני
audit-classifications.jsonl  ביקורת v3 מלאה
rehome-suggestions.jsonl     הצעות מעבר
orphan-sides.jsonl           צדדים מאומתים
```
בסקרצ'פד: `audit-v1.jsonl`, `audit-v2.jsonl` — סבבים קודמים, לצורך השוואה.

---

## 8. דיפלוי — איך זה עובד כאן

### שלושה יעדי דחיפה, תמיד יחד

```bash
git push origin dina          # ענף העבודה
git push origin dina:dev      # dev בריפו הראשי
git push fork dina:main       # main בפורק של דינה
```

`origin` = `knesset-watch/knesset.watch` (הריפו המשותף)
`fork`   = `dinafk-cell/knesset.watch` (הפורק של דינה)

`dina` ו-`origin/dev` מוחזקים זהים. **`origin/main` הוא ענף נפרד שארבל
עובדת מולו, והוא מפגר** — נכון ל-19.9.2026 יש בו 2 קומיטים שלה ו-53
קומיטים שלנו חסרים בו.

### הבנייה מגישה מסד אחר

```json
// apps/knesset-watch/vercel.json
"buildCommand": "cp knesset-deploy.db knesset.db && npm run build"
```

אותה פקודה מוגדרת גם ב-Build Command של Render. כלומר **הפרודקשן אינו
רץ על `knesset.db` המקומי** אלא על `knesset-deploy.db`, שהוא קובץ בן 97MB
**במעקב גיט** (חריג ל-gitignore, ראו `.gitignore` שורה 37).

### מכאן נובע הכלל החשוב ביותר

> **שינוי במסד אינו מגיע לפרודקשן עד שמריצים `build-deploy-db.ts` ודוחפים
> את הקובץ.**

```bash
npx tsx scripts/build-deploy-db.ts    # מעתיק מ-knesset.db ל-knesset-deploy.db
git add apps/knesset-watch/knesset-deploy.db
# ואז שלוש הדחיפות
```

הסקריפט מסיים בהשוואת ספירות מול `knesset.db` ומדפיס ✓ או ✗ לכל טבלה.
**אם יש ✗ — לא לדחוף.** הבדיקה הזו נוספה אחרי שהתגלה שהסקריפט לא העתיק
כלל את `bill_political_classification` במשך חודשיים, והבדיקות הישנות
("הטבלה קיימת", "ההצטרפות עובדת") עברו בהצלחה כל אותו זמן.

GitHub מזהיר על הגודל (`GH001: Large files detected`) — **זה אזהרה ולא
שגיאה**, הדחיפה מצליחה.

### מה עובר בגיט ומה לא

| עובר | לא עובר |
|---|---|
| `knesset-deploy.db` (97MB) | `knesset.db` המקומי (232MB) |
| קוד, `axis-catalog.json`, `axis-clusters.json` | `.env.local`, מפתחות API |
| | כל קובצי ה-CSV וה-XLSX (ב-gitignore) |

### אימות אחרי דיפלוי

האתר **https://knesset.watch** מאחורי `AUTHORIZE`. אין דרך לאמת אוטומטית
מכאן — צריך להיכנס ידנית ולבדוק.

### Render — נבדק ואומת 19.9.2026

```
שירות    afarkeset-laknesset   (srv-dakff71594qs73dt77f0, Node, Free)
ריפו     dinafk-cell/knesset.watch
ענף      main                  ← כלומר "fork/main" בריפו הזה
כתובת    https://afarkeset-laknesset.onrender.com
```

**הפרודקשן הוא `main` של הפורק האישי של דינה, לא הריפו המשותף.**

**Auto-Deploy דלוק.** אומת: הקומיט החי היה הקומיט האחרון שנדחף, דקות
אחרי הדחיפה. כלומר `git push fork dina:main` הוא **פריסה לאוויר**, לא
רק דחיפה לגיט.

> **המשמעות התפעולית:** כל דחיפה ל-`fork main` עולה לפרודקשן מיד, בלי
> סקירה ובלי PR. בסשן של 18–19.9 נדחפו כך עשרות קומיטים. אין בכך פסול
> אם זה מכוון — אבל צריך לדעת את זה לפני שדוחפים.

**האתר אינו מוגן בסיסמה.** נפתח חופשי. דינה הסירה את `SITE_PASSWORD`
מ-Render בשלב מוקדם של הסשן.

**מה שעדיין לא נבדק בדשבורד:** ה-Build Command בפועל. לפי `vercel.json`
ולפי הערה ב-`build-deploy-db.ts` הוא
`cp knesset-deploy.db knesset.db && npm run build`, וזה עקבי עם העובדה
שהנתונים החדשים מופיעים באתר — אבל לא נראה בעיניים.

### מה מזה נובע לגבי הפיצ'ר של ארבל

ארבל מיזגה את נוכחות הוועדות ל-`main` של **הריפו המשותף**
(`knesset-watch/knesset.watch`), שאינו מקור הפריסה. **הפיצ'ר שלה לא
יעלה לאוויר** עד שהוא יגיע ל-`main` של הפורק של דינה.

---

## 9. פתוח

1. **1,969 השינויים** — לאשר, לדחות נקודתית, או לפצל (מעברים עכשיו, מחיקות אחרי הצירים החסרים)
2. **249 מחיקות ב"ביטחון בינוני"** — יש להן ציר מועמד ורק הוודאות חסרה. אפשר לסקור במקום למחוק
3. **1,020 היתומות** — דינה עוברת עליהן ידנית
4. **עשרת הצירים עם 4 הצעות** שנפלו בעבר מתחת לסף — הדרך אליהם פתוחה עכשיו
5. **מיזוג הפיצ'ר של ארבל** — נוכחות בוועדות. היא הסתעפה מ-PR #24 וכתבה מחדש
   1,513 שורות ב-`MKProfileClient.tsx`. **53 קומיטים שלנו אינם ב-main.**
   שלושה קבצים מתנגשים. דינה ביקשה מארבל למזג.
6. **הלוגו** — דינה אמרה שאינו נראה טוב. פאביקון לא נוצר.
