{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "cd ../.. && npm install",
  "framework": "nextjs",
  "regions": ["fra1"],
  "ignoreCommand": "[ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ] && exit 1; git diff --quiet $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA -- apps/knesset-watch/ packages/"
}
