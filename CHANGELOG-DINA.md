# יומן שינויים — ענף `dina`

מסמך מתעדכן. כל שינוי שנעשה באפליקציה המקורית, למה, ומה נמדד כדי לאמת אותו.

**עדכון אחרון:** 2 בספטמבר 2026
**ענף:** `dina` · **מוזג ל-`dev`:** עד `36fb316` בלבד

---

## איך לקרוא את המסמך

- **✅ ב-dev** — השינוי מוזג ונמצא גם ב-`main`
- **🔵 dina בלבד** — קיים רק בענף שלי, טרם מוזג
- כל מספר במסמך נמדד ישירות מול `knesset.db` או מול הקוד. אין הערכות.

---

## פיצ'רים חדשים

### מדד מעורבות פרלמנטרית ✅ ב-dev
`src/lib/knesset-db.ts` · commit `9f53235`

מודד כמה ח"כ פעיל בעבודה הפרלמנטרית. שני רכיבים שווים: מספר הצעות החוק שיזם, ושיעור ההשתתפות בהצבעות. כל רכיב מומר לאחוזון לפני השקלול, כי הם בסקאלות שונות לגמרי — בלי ההמרה מספר ההצעות היה מכריע לבדו.

לא מודד איכות חקיקה, אג'נדה או כוח פוליטי. שרים מקבלים ציון אפסי מעצם ההגדרה, ולכן יש דגל `isMinister` שהתצוגה חייבת להסביר.

### מדד מעורבות באג'נדה ✅ ב-dev
`src/lib/agenda-activity.ts` · commits `91954d1`, `904a98a`, `6105547`

ציון נפרד לכל צמד ח"כ×אג'נדה:

```
AgendaActivity = 0.6 × אחוזון יוזמה חקיקתית + 0.4 × אחוזון תמיכה בהצבעות
```

נספרות **רק** הצעות חוק והצבעות שדוחפות לכיוון שהמשתמש בחר. ח"כ שיזם עשרה חוקים להרחבת הפטור מגיוס אינו "עובד למען" משתמש שתומך בגיוס.

כשאין נתוני עמדה (`bill_political_classification` חסרה), המדד מציג פעילות בנושא בלי כיוון, והתצוגה אומרת זאת במפורש במקום להבטיח התאמה שאין.

### פיצ'ר ההתאמה ✅ ב-dev
`src/app/agenda-match/` · `src/app/api/agenda-activity/` · commits `0dffead`, `b8a31fc`, `e9988f7`

שאלון בשלושה שלבים: תחום ← עמדה ← תוצאות. השלב הראשון יושב גם במסך הבית ומעביר את הבחירה ב-`?domains=`. החיפוש נכתב ל-`?picks=issue:stance`, כך שיציאה להצעת חוק וחזרה אחורה מחזירה את אותה רשימה.

היררכיית גוונים: תחום בטורקיז, אג'נדה באינדיגו. כל ח"כ מציג את הצעות החוק עצמן כקישורים ל-`/bill/[id]` עם תגית "עבר" או "התקדם".

### תמונות ורקע של חברי כנסת 🔵 dina בלבד
`src/lib/mk-profiles.ts` · `scripts/fetch-mk-profiles.ts`

תמונה ושורת רקע ("מחנך · ישיבת נווה שמואל") לצד כל ח"כ בתוצאות.

**המקור:** Wikidata. לא הכנסת — ל-OData שלה אין תמונות ואין ביוגרפיה באף אחת מ-48 הישויות, ואתר הכנסת הציבורי חסום ב-WAF שדורש JavaScript ומחזיר HTTP 247.

| | |
|---|---|
| ח"כים עם פרופיל | 140 מתוך 147 (95%) |
| עם תמונה | 139 |
| עם עיסוק | 133 |
| עם השכלה | 119 |
| גודל הקובץ | 53KB |

הנתונים נשלפים מראש ל-JSON סטטי — אין קריאת רשת בזמן ריצה. שבעת החסרים נופלים חזרה לראשי תיבות צבעוניים.

---

## תיקוני באגים

### ספירת הצעות חוק באג'נדות הראתה אפס בכל מקום 🔵 dina בלבד
commit `f5eecef`

שתי שאילתות סיננו על `bill.macro_agenda`, שריק בכל 7,296 השורות, ולכן החזירו תמיד רשימה ריקה:

- **`/api/agendas`** — כל אחד עשר הכרטיסים הציגו "0 הצעות חוק" לצד מאות הצבעות
- **`getMkAgendaStats`** — עמוד הפרופיל הציג אפס בכל נושא לכל ח"כ, כולל אחד שיזם 399

**התיקון:** החוק יורש את האג'נדה מההצבעה שנערכה עליו. `plenary_vote` מתויג ב-100% ומחזיק `bill_id`.

| אג'נדה | לפני | אחרי |
|---|---|---|
| מנהל ומשפט | 0 | 354 |
| ביטחון וצבא | 0 | 169 |
| דת ומדינה | 0 | 95 |

`plenary_vote.bill_id` חסר ב-`knesset-deploy.db` שנפרס בפרודקשן, ולכן נבדק לפני השימוש.

### מודל Gemini ✅ ב-dev
commit `423c280` (נדרס במיזוג)

מיגרציה ל-`gemini-3.6-flash` עם `thinkingLevel`. בפועל התקבלה הגרסה של `dev` (`gemini-3.5-flash`) בהחלטה מודעת. אם 3.5 ייכשל — לשם צריך לחזור.

---

## היגיינה

### הוצאת קבצים בינאריים מ-git 🔵 dina בלבד
commit `f487a2a` · **לא מוזג ל-dev בכוונה**

`dev` קיבל 7,213 קבצי PDF ושני קבצי SQLite של 81MB, שלא היו מכוסים ב-`.gitignore` בניגוד למוסכמה ב-README. ה-`.git` הגיע ל-477MB.

ה-commit מוציא אותם מהמעקב ומשאיר על הדיסק. **הוא לא מוזג ל-`dev` בכוונה** — מיזוגו היה מוחק את הקבצים אצל ארבל וזויה. העבודה נכנסה ל-`dev` דרך `feature/agenda-activity`, שאין בו מחיקות.

אם קבצים בכל זאת נעלמים אחרי החלפת ענף:

```bash
git restore --source=origin/dev --worktree -- apps/knesset-watch/bill-documents/
```

הפקודה נבדקה בפועל — משחזרת 7,407 קבצים מיידית, בלי הורדה מחדש.

---

## חוב טכני שנרשם ולא בוצע

| # | מה | היכן |
|---|---|---|
| 1 | 42% ממילות המפתח מחזירות אפס תוצאות, כולל `"גיוס חרדים"` | `src/lib/agendas.ts` |
| 2 | `"בג״ץ"` כתוב בגרשיים עבריים ולא תואם למאגר — 0 תוצאות | `src/lib/agendas.ts` |
| 3 | `confidence` נשמר בסיווג ואף אחד לא מסנן לפיו | `src/lib/compute-match.ts` |
| 4 | `MAX_TEXT_LENGTH = 900` — המודל רואה 49% מהחוק החציוני ו-1% מהארוך | `scripts/classify-bill-agendas-llm.ts` |
| 5 | אין `knesset_num` על `bill`, `bill_initiator`, `mk_person`, `plenary_vote` — סנכרון כנסת נוספת יערבב בשקט | סכימה |
| 6 | `bill_id` בהצבעות היוריסטי ב-100%, 456 מסומנות `ambiguous` | `plenary_vote` |
| 7 | הנתונים לא ב-git — הסיווג רק אצל ארבל, 194 חוקים רק אצל זויה | תהליך |
| 8 | `knesset-deploy.db` שנפרס ישן מ-`knesset.db` וחסר בו `plenary_vote.bill_id` | `vercel.json` |

---

## מספרי ייחוס

נמדדו מול `knesset.db`, כנסת 25 בלבד, 16.11.2022–25.2.2026.

| | |
|---|---|
| הצעות חוק | 7,296 · מתוכן 502 עברו |
| הצבעות מליאה | 6,358 · 100% מתויגות `macro_agenda` |
| רשומות הצבעה של ח"כים | 432,363 |
| **משמעת סיעתית** | **99.83%** — 709 חריגות בלבד |
| ח"כים מכהנים | 120 · מתוכם 24 שרים |
| `bill.macro_agenda` / `summary` / `doc_url` | **0%** — ריקים לגמרי |
| חוקים עם PDF | 7,213 מתוך 7,296 |
