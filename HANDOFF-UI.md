# מסירה: שיפור ה-UI של knesset.watch

מסמך פתיחה לצ'אט חדש. נכתב 9.9.2026. כל המספרים כאן נמדדו, לא נזכרו.

---

## 1. מה זה הפרויקט

`knesset.watch` — אתר שעוקב אחרי הכנסת: חברי כנסת, הצעות חוק, הצבעות,
ועדות, פרוטוקולים. פרויקט גמר של שלוש: **דינה** (בעלת הרפו, עובדת מול הצ'אט
הזה), **זויה** ו**ארבל**.

**מיקום:** `C:\Users\User\projects\knesset.watch`
לא `C:\Users\User\final project` — התיקייה הזאת ריקה.

**הרצה:**
```bash
cd "C:/Users/User/projects/knesset.watch/apps/knesset-watch" && npm run dev
```
פורט **3001**. `next dev --webpack`.

הערה: `better-sqlite3` טוען את הסכמה פעם אחת בעליית המודול. אחרי שינוי
במסד הנתונים צריך להפעיל מחדש את השרת, ולעיתים גם `rm -rf .next`.

---

## 2. הערימה

| | |
|---|---|
| Next.js | 16.1.6, App Router |
| React | 19.2.3 |
| Tailwind | 4.2.2 |
| TypeScript | כן, `npx tsc --noEmit` נקי |
| מסד נתונים | SQLite מקומי (`better-sqlite3`) + Turso ל-פרוטוקולים |
| שפה | עברית, RTL |

---

## 3. מצב העיצוב — כאן ההזדמנות

**`src/app/globals.css` הוא 15 שורות.** זה כל מערכת העיצוב:

```css
@import "tailwindcss";
:root { --background: #ffffff; --foreground: #000000; }
```

כלומר: **אין טוקנים, אין סקאלת צבע, אין סקאלת מרווחים, אין רכיבי בסיס.**
כל עמוד כתוב במחלקות Tailwind בשורה, וכל אחת מהשלוש כתבה בסגנון משלה.
זה המקור העיקרי לחוסר האחידות.

**`src/app/layout.tsx`:**
```tsx
<html lang="he" dir="rtl">
<body className="font-serif antialiased bg-white">
```
שני גופנים מ-Google: `Source_Serif_4` ו-`Frank_Ruhl_Libre` (עברית).
כל האתר ב-serif. רקע לבן קשיח.

---

## 4. המפה

**25 מסלולים:**

```
/                    דף הבית (KnessetWatchClient — 1,809 שורות)
/mks  /mk/[id]       רשימת חכ"ים + כרטיס אישי (MKProfileClient — 1,507 שורות)
/bills  /bill/[id]   הצעות חוק
/votes  /vote/[id]   הצבעות
/committees  /committee/[name]  /session/[id]
/factions דרך /faction/[name]
/ministers  /ministry/[name]  /office/[slug]
/agendas  /agenda/[topic]
/agenda-keywords     ← השאלון החדש (הפיצ'ר המרכזי)
/agenda-match        ← השאלון הישן, מיועד למחיקה
/ask  /protocols  /search  /pulse  /track-record  /status  /login
```

**11 רכיבים משותפים** ב-`src/components/`:
`AppSidebar`, `SiteHeader`, `MkIdentity`, `MkTimeline`, `FilterChips`,
`EntityTooltip`, `GlossaryTooltip`, `AllianceGraph`, `PresenceHeatmap`,
`TimelineChart`, `VoteCoalition`.

**39 רכיבי `'use client'`**, 13,122 שורות TSX סה"כ.

**חמשת הקבצים הגדולים** — מועמדים ראשונים לפירוק:
```
1,809  src/app/KnessetWatchClient.tsx
1,507  src/app/mk/[id]/MKProfileClient.tsx
  755  src/app/agenda-match/AgendaMatchClient.tsx   ← ממילא למחיקה
  716  src/components/AllianceGraph.tsx
  627  src/app/agenda-keywords/KeywordMatchClient.tsx
```

---

## 5. הפיצ'ר המרכזי — השאלון

`/agenda-keywords` — "מי עובד בשבילך". המשתמש בוחר תגיות, מסמן עמדה,
ומקבל דירוג חברי כנסת. נבנה בארבעה שלבים: 8 נושאי-על → 52 אשכולות →
166 תגיות → דירוג.

מגבלות בקוד: `MAX_TOPICS = 3`, `MAX_CLUSTERS = 6`.

**היסטוריית ה-UX שלו — חשוב לא לחזור אחורה.** דינה בדקה אותו שוב ושוב
ופסלה שלוש גרסאות:
1. שאלות ארוכות → *"יש יותר מדי אג'נדות, זה מעייף ולא ריאלי"*
2. ענן מילות מפתח → *"סליחה, זה גם עמוס מדי"*
3. תגיות מרובות → *"התגיות יכולות לאחד בתוכן יותר תגיות"* — האבחנה
   שלה הייתה שהתגיות עצמן משכפלות זו את זו. לדוגמה שנתנה: "קצבאות נכות
   ונגישות", "אנשים עם מוגבלויות", "הנגשה ותמיכה לנכים" — שלוש תגיות,
   נושא אחד.

הגרסה הנוכחית עברה: *"עכשיו נראה ממש טוב וידידותי"*.
**אל תוסיפו עומס חזרה לשלב 2.**

`/agenda-match` היא הגרסה הישנה. דינה ביקשה להשאיר אותה כגיבוי. אפשר
להציע למחוק אותה עכשיו.

---

## 6. אילוצי נתונים שמשפיעים על התצוגה

אלה נמדדו ואינם ניתנים לשינוי מה-UI:

- **`status_desc` ריק בכל 7,296 הצעות החוק.** שלב החוק נגזר מ-`status_id`
  בלבד, ב-`src/lib/bill-stage.ts`.
- **`doc_url` ריק ב-100%.** כפתור ה-PDF היה קוד מת והוסר.
- **רק 409 מתוך 4,660 חוקים מסווגים הגיעו להצבעה במליאה.** לכן הדירוג
  נשען על יוזמה (משקל 0.6) ולא רק על הצבעות (0.4).
- **אחוז הביטחון מוצג למשתמש** ודורש הסבר לידו: חכ"ים מהקואליציה
  יקבלו מטבע הדברים ביטחון גבוה יותר. ההסבר כבר בקוד — אל תסירו אותו.
- **150 חברי כנסת**, 149 תמונות (חסרה של עוז חיים — אתר הכנסת חוסם),
  150 מקצועות, 109 תארים.
- חכ"ים לשעבר מוצגים עם תג "כיהן עד ...".

---

## 7. מוסכמות עבודה עם דינה

היא כתבה אותן במפורש ואכפה אותן לאורך כל העבודה:

1. **לשאול לפני כל שינוי.** לאבחן, להציע, ואז לחכות. לא לפעול ואז לדווח.
2. **לעבוד על ענף `dina`** אלא אם נאמר אחרת.
3. **לא לעשות commit מהצ'אט** בלי בקשה מפורשת.
4. **לא להסתיר נתונים.** אם משהו נכשל — לומר.
5. לאמת על ריצה קטנה לפני ריצה ארוכה.
6. **היא כותבת בעברית ומצפה לתשובות בעברית.**

היא שמה לב לפרטים ותופסת טעויות — כולל כאלה שהצ'אט הזה עשה. עדיף
למדוד מאשר לנחש, ולומר "בדקתי" רק כשבאמת בדקת.

---

## 8. מצב הענפים כרגע

```
origin/main   946d3d2   כולל את כל קוד הפיצ'ר
origin/dev    9d9a55f
dina          22e44d2   ← הענף לעבודה
```

⚠️ **ב-`dina` יש 19 קומיטים שלא נדחפו.** כל העבודה של הימים האחרונים
קיימת רק על הדיסק. כדאי לדחוף:
```bash
git push origin dina
```

**שני PR פתוחים** (לא קשורים ל-UI, אפשר להתעלם):
- `deploy-db-add-new-mks` — נתוני פרודקשיין
- `fix-ci-darwin-only-dep` — תיקון ל-CI שנכשל

---

## 9. נקודות פתיחה מוצעות לשיפור ה-UI

לפי סדר התשואה:

1. **מערכת עיצוב.** 15 שורות CSS זה שורש חוסר האחידות. טוקנים לצבע,
   מרווח וטיפוגרפיה ב-`globals.css` ייתנו בסיס לכל השאר.
2. **פירוק `KnessetWatchClient.tsx`** (1,809 שורות) ו-`MKProfileClient.tsx`
   (1,507). שניהם עשו הרבה מדי.
3. **מובייל.** לא נבדק כלל בעבודה עד כה. סביר שיש שם בעיות.
4. **נגישות.** `aria`, ניגודיות, ניווט מקלדת — לא נבדקו. באתר על
   דמוקרטיה זה גם עניין מהותי.
5. **מצבי טעינה וריק.** לא ידוע אם קיימים באופן עקבי.

**המלצה:** לפתוח בסקירה של המצב הקיים בדפדפן לפני שנוגעים בקוד, ולהציג
לדינה ממצאים לפני שינוי — זה מה שהיא מצפה לו.
